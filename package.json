{
  "name": "electron-app",
  "main": "app/main.js",
  "scripts": {
    "build": "asar pack app dist/resources/app.asar && start dist/electron.exe",
    "start": "rm -f dist/resources/app.asar && start dist/electron.exe app/main.js"
  },
  "devDependencies": {
    "asar": "^0.11.0"
  }
}
